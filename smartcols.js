/*
 *  jQuery code to adjust viewport width for a whole number of cards
 */
$(document).ready(function(){
    
    function cols() {
    }   
    
    cols(); 

    $(window).resize(function () {
        cols();
    }); 
});
